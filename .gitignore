node_modules/
dist/
*.log
.DS_Store
packages/core/dist/
packages/vscode/dist/
# Webview assets are built by CI before packaging (see .github/workflows/vscode-publish.yml),
# mirroring packages/intellij/.gitignore. A committed copy is always stale against src/.
packages/vscode/webview/
*.vsix
.idea/

# Throwaway UI review screenshots (Playwright captures). Not part of the tracked README assets.
review-screenshots/

# Scratch corpus written by scripts/generate-task-parser-corpus.ts. Generated fixtures are
# throwaway input for a differential run; adjudicated findings are committed to
# test-fixtures/task-parser/ as authored fixtures instead.
.task-parser-scratch/
