<idea-plugin>
    <id>tw.kewang.spek</id>
    <name>spek - OpenSpec Viewer</name>
    <vendor url="https://github.com/spekhq/spek">kewang</vendor>
    <description><![CDATA[
        <p><b>spek</b> is an OpenSpec content viewer that lets you browse specs, changes, and task progress directly within your IDE.</p>

        <p><b>Features:</b></p>
        <ul>
            <li><b>Specs Browser</b> — Browse spec documents with BDD syntax highlighting (WHEN/GIVEN, THEN, AND, MUST/SHALL)</li>
            <li><b>Changes Browser</b> — View changes with a tab per artifact the change contains (custom OpenSpec schemas supported), with the change's schema shown as a badge; rows surface creation/archive dates and lifecycle duration</li>
            <li><b>Timeline</b> — Horizontal Gantt-style chart of every change's lifecycle, with optional spec-topic grouping and an auto-scaling time axis</li>
            <li><b>Task Progress</b> — Track task completion with section-grouped progress bars</li>
            <li><b>Graph View</b> — Interactive force-directed graph showing spec-change relationships</li>
            <li><b>Full-text Search</b> — Search across all specs and changes</li>
            <li><b>Spec Diff</b> — Compare current spec content against historical change versions</li>
            <li><b>Live Reload</b> — Automatically refreshes when openspec files change on disk</li>
            <li><b>Collapsible Tree Navigator</b> — A Specs / Changes tree sits above the viewer. Hide it from the tool window title bar or the gear menu to give the viewer the whole tool window; the choice and the split ratio are remembered per project</li>
            <li><b>Dark / Light Theme</b> — Syncs with your IDE theme</li>
        </ul>

        <p><b>Supported IDEs:</b> IntelliJ IDEA, WebStorm, PyCharm, PhpStorm, GoLand, RubyMine, CLion, Rider, and other IntelliJ-based IDEs (2023.3+)</p>

        <p><b>Getting Started:</b> Open a project that contains an <code>openspec/</code> directory. The spek tool window will appear in the right sidebar.</p>
    ]]></description>

    <change-notes><![CDATA[
        <p><b>1.12.0</b></p>
        <p><b>spek now shows the workflow itself, not just its output.</b> A schema decides what a change <i>is</i> in OpenSpec &mdash; which artifacts exist, what order they come in, what each is supposed to contain, and when the change is ready to implement. Until now it appeared only as a small badge on a change that differed from the project default: a name with nothing behind it.</p>
        <ul>
            <li><b>New Schemas page</b> listing every workflow schema available to the project, with its description, its source (shipped with the <code>openspec</code> package, or project-local under <code>openspec/schemas/</code>), and how many artifacts it defines. The schema named by <code>openspec/config.yaml</code> is marked as the default, and each schema shows how many active changes declare it, with a link through to them. The plugin reads schemas through its own Kotlin implementation, aligned with the same rules. Thanks to <a href="https://github.com/nthansen">@nthansen</a> (Norman Hansen).</li>
            <li><b>A schema detail view that reads as a workflow</b> &mdash; its artifacts in authoritative order as a diagram, each showing the file it generates, what it requires before it can be written, and its full instruction text. The <code>apply</code> step is drawn as the flow's terminal step.</li>
            <li><b>When the OpenSpec CLI cannot answer, the page says so.</b> If the CLI is missing, exits non-zero, times out, or emits something unparsable, the list comes back empty <i>with the reason stated</i>, rather than as an unexplained empty page.</li>
            <li><b>Spec content is now ranked by structure rather than by inherited type size.</b> In a change's Specs tab the spec's own name was smaller than the <code>ADDED Requirements</code> label inside it, so a reader scrolled past the boundary between two stacked specs without registering that one occurred. The topic name is now the dominant heading of its section, the operation label is demoted, and an open section is inset with a hairline rule marking where it ends. Reported from this tool window, where a narrow width makes the ranking matter most (issue #42).</li>
            <li><b>Every delta operation is marked, not an arbitrary two.</b> <code>REMOVED</code> and <code>RENAMED</code> had no styling of their own, so in prose they read as ordinary words. <code>REMOVED</code> deliberately does not take red: red already means "normative" (<code>MUST</code> / <code>SHALL</code>) here.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <!-- Since 2026.2 (262) JCEF ships as the bundled plugin `com.intellij.modules.jcef`, whose content modules
         (intellij.platform.ui.jcef = com.intellij.ui.jcef.*, intellij.libraries.jcef = org.cef.*) each get their
         own classloader. An old-format descriptor that does not declare this cannot see JBCefApp and throws
         NoClassDefFoundError when the Tool Window opens (issue #24). Declared optional: IDEs older than 262 have no
         such plugin id, and an unresolved optional depends is not a strict dependency, so loading on 2023.3+ is
         unaffected. -->
    <depends optional="true" config-file="spek-jcef.xml">com.intellij.modules.jcef</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="spek"
                    anchor="right"
                    icon="/icons/spek.svg"
                    factoryClass="com.spek.intellij.SpekToolWindowFactory"/>

        <httpRequestHandler implementation="com.spek.intellij.server.SpekHttpRequestHandler"/>

        <postStartupActivity implementation="com.spek.intellij.SpekStartupActivity"/>
    </extensions>

    <actions>
        <action id="spek.open"
                class="com.spek.intellij.actions.OpenSpekAction"
                text="Open spek"
                description="Open OpenSpec viewer">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
