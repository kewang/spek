<idea-plugin>
    <id>tw.kewang.spek</id>
    <name>spek - OpenSpec Viewer</name>
    <vendor url="https://github.com/spekhq/spek">kewang</vendor>
    <description><![CDATA[
        <p><b>spek</b> is an OpenSpec content viewer that lets you browse specs, changes, and task progress directly within your IDE.</p>

        <p><b>Features:</b></p>
        <ul>
            <li><b>Specs Browser</b> — Browse spec documents with BDD syntax highlighting (WHEN/GIVEN, THEN, AND, MUST/SHALL)</li>
            <li><b>Changes Browser</b> — View changes with a tab per artifact the change contains (custom OpenSpec schemas supported), with the change's schema shown as a badge; rows surface creation/archive dates and lifecycle duration</li>
            <li><b>Timeline</b> — Horizontal Gantt-style chart of every change's lifecycle, with optional spec-topic grouping and an auto-scaling time axis</li>
            <li><b>Task Progress</b> — Track task completion with section-grouped progress bars</li>
            <li><b>Graph View</b> — Interactive force-directed graph showing spec-change relationships</li>
            <li><b>Full-text Search</b> — Search across all specs and changes</li>
            <li><b>Spec Diff</b> — Compare current spec content against historical change versions</li>
            <li><b>Live Reload</b> — Automatically refreshes when openspec files change on disk</li>
            <li><b>Collapsible Tree Navigator</b> — A Specs / Changes tree sits above the viewer. Hide it from the tool window title bar or the gear menu to give the viewer the whole tool window; the choice and the split ratio are remembered per project</li>
            <li><b>Dark / Light Theme</b> — Syncs with your IDE theme</li>
        </ul>

        <p><b>Supported IDEs:</b> IntelliJ IDEA, WebStorm, PyCharm, PhpStorm, GoLand, RubyMine, CLion, Rider, and other IntelliJ-based IDEs (2023.3+)</p>

        <p><b>Getting Started:</b> Open a project that contains an <code>openspec/</code> directory. The spek tool window will appear in the right sidebar.</p>
    ]]></description>

    <change-notes><![CDATA[
        <p><b>1.11.0</b></p>
        <p><b>A spec opens as an outline with substance, instead of a wall of text.</b> Reported from this plugin (issue #42): "you are asked to read all these fine details, but you don't even really know the shape of the thing first." A tool window is narrower than a browser window, which made the wall worse here than anywhere else.</p>
        <ul>
            <li><b>Requirements and scenarios fold in place.</b> Each <code>### Requirement:</code> shows its heading <i>and</i> its lead SHALL paragraph; each <code>#### Scenario:</code> shows as a heading with its WHEN/THEN body collapsed. Scenario blocks are 59% of the character volume in a typical spec, so the first screen becomes a contents page that still says what each requirement requires. Expand all / Collapse all is available, and your choice is remembered. This does <b>not</b> depend on the tool window's width, unlike the table of contents, which stays hidden below 1280px.</li>
            <li><b>Find-in-page still finds folded text, and links still land.</b> Folding uses the browser's native disclosure elements rather than hiding content with CSS, so a find reaches text inside a collapsed scenario. Navigating to a heading expands whatever encloses it before scrolling, so a link never arrives at something invisible.</li>
            <li><b>Folding applies only to spec-shaped content</b> &mdash; the spec detail page and a change's Specs tab. Proposal, design and other markdown artifacts render unfolded exactly as before.</li>
            <li><b>Every BDD keyword is legible in the light theme.</b> WHEN / THEN / SHALL and the rest were hard-coded to one set of colours shared by both themes, and against the light background all 8 failed WCAG AA &mdash; 7 below even 3:1, with THEN at 1.43:1. The dark theme passed everywhere, which is why this went unseen. Light now has its own values in the same hue families, clearing AA at 5.17&ndash;6.47:1; dark is unchanged byte for byte, and no hue or pill fill moves on either theme.</li>
            <li><b>A highlighted keyword no longer renders lighter than the emphasis around it.</b> A keyword inside <code>**bold**</code> was drawn at a lower font weight than the bold text containing it.</li>
            <li><i>Internal:</i> the Kotlin task parser is now verified against the same shared fixture corpus as the TypeScript one, so a case added in either language is asserted by both from the next run. This is what keeps the two from drifting apart on the same tasks.md.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <!-- Since 2026.2 (262) JCEF ships as the bundled plugin `com.intellij.modules.jcef`, whose content modules
         (intellij.platform.ui.jcef = com.intellij.ui.jcef.*, intellij.libraries.jcef = org.cef.*) each get their
         own classloader. An old-format descriptor that does not declare this cannot see JBCefApp and throws
         NoClassDefFoundError when the Tool Window opens (issue #24). Declared optional: IDEs older than 262 have no
         such plugin id, and an unresolved optional depends is not a strict dependency, so loading on 2023.3+ is
         unaffected. -->
    <depends optional="true" config-file="spek-jcef.xml">com.intellij.modules.jcef</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="spek"
                    anchor="right"
                    icon="/icons/spek.svg"
                    factoryClass="com.spek.intellij.SpekToolWindowFactory"/>

        <httpRequestHandler implementation="com.spek.intellij.server.SpekHttpRequestHandler"/>

        <postStartupActivity implementation="com.spek.intellij.SpekStartupActivity"/>
    </extensions>

    <actions>
        <action id="spek.open"
                class="com.spek.intellij.actions.OpenSpekAction"
                text="Open spek"
                description="Open OpenSpec viewer">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
