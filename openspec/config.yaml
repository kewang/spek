schema: spec-driven

context: |
  Project: spek — OpenSpec Viewer
  Tech stack: TypeScript, React 19, Vite, Express, Tailwind CSS v4
  Purpose: A local read-only web app for browsing the contents of a repo that has an openspec/ directory
  Conventions: English is the single source of truth for everything committed — code, comments, openspec/ artifacts, docs
